export class Ingredient {

    /* NOTE: typescript has a feature where you can automatically declare and
    initialize class variables in the constructor! */
    constructor(public name: string, public amount: number){
    }
}